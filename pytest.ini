[pytest]
# 运行参数：显示详细信息，使用 allure 生成测试结果，控制控制台输出等
addopts = -v -s --alluredir=outputs/reports --disable-warnings --color=yes

# 测试用例所在的目录
testpaths = tests

# 测试用例文件命名规则
python_files = test_*.py

# 测试类命名规则
python_classes = Test*

# 测试函数命名规则
python_functions = test_*

# 将当前目录加入 PYTHONPATH 确保可以正确导包
pythonpath = .

# 注册自定义标记
markers =
    smoke: 冒烟测试
    login: 登录模块测试
    audit: 审核模块测试
    register: 注册模块测试
